
# Column headings and their expected data types based on the imported data.
# Note: This schema can be changed based on the imported data.
expected_schema = {
    "date": 'object',
    "datetime": "datetime64[ns]",
    "cash_type": "object",
    "card": "object",
    "money": "float64",
    "coffee_name": "object",
}
